#:schema node_modules/wrangler/config-schema.json
name = "fetcher"
main = "src/worker.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# Resolve workspace SDK directly from source (no dist build needed)
[alias]
"@lemmaoracle/sdk" = "../sdk/src/index.ts"
