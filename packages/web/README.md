# Lemma

This is a code bundle for Lemma. The original project is available at https://www.figma.com/design/7H5ah29125QtPvi1dQjoER/Lemma.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
