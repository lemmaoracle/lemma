---
import TopV47Template from "../templates/TopV47Template.astro";
import { getTranslations } from "../i18n/translations";

const locale = "en";
const t = getTranslations(locale);
const base = "";
---

<TopV47Template locale={locale} t={t} base={base} />
