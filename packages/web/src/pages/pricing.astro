---
/* Pricing。JA と同じ v12 テンプレ（断片は EN 版）。 */
import PricingV12Template from "../templates/PricingV12Template.astro";
import { getTranslations } from "../i18n/translations";

const locale = "en" as const;
const t = getTranslations(locale);
const base = "" as const;
---

<PricingV12Template locale={locale} t={t} base={base} />
