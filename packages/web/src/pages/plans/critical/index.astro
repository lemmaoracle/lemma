---
import PlanTemplate from "../../../templates/PlanTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "en" as const;
const t = getTranslations(locale);
const base = "" as const;
---

<PlanTemplate locale={locale} t={t} base={base} planKey="critical" />
