---
import Trust402SellModelTemplate from "../../../templates/Trust402SellModelTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "en";
const t = getTranslations(locale);
const base = "";
---

<Trust402SellModelTemplate locale={locale} t={t} base={base} />
