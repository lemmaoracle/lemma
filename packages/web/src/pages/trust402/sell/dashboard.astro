---
import Trust402SellDashboardTemplate from "../../../templates/Trust402SellDashboardTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "en";
const t = getTranslations(locale);
const base = "";
---

<Trust402SellDashboardTemplate locale={locale} t={t} base={base} />
