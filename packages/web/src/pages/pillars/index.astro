---
// /pillars — the canonical "Lemma API 全体像" hub (code納品 20260707). The old
// pillars/4-axes hub was retired; the thin /lemma-api route now redirects here.
import LemmaApiTemplate from "../../templates/LemmaApiTemplate.astro";
import { getTranslations } from "../../i18n/translations";

const locale = "en";
const t = getTranslations(locale);
const base = "";
---

<LemmaApiTemplate
  locale={locale}
  t={t}
  base={base}
  pagePath="/pillars/"
  ogImage="https://lemma.frame00.com/og/pillars/en.png"
/>
