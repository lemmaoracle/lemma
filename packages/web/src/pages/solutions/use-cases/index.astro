---
/* Use cases index。JA と同じ v14 テンプレ（断片は EN 版）。 */
import UseCasesIndexV14 from "../../../templates/UseCasesIndexV14.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "en" as const;
const t = getTranslations(locale);
const base = "" as const;
---

<UseCasesIndexV14 locale={locale} t={t} base={base} />
