---
import SolutionsTemplate from "../../../templates/SolutionsTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<SolutionsTemplate locale={locale} t={t} base={base} />
