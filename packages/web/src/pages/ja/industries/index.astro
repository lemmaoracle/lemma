---
/* Industries landing。コピーと CSS は templates/IndustriesTemplate。 */
import IndustriesTemplate from "../../../templates/IndustriesTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "ja" as const;
const t = getTranslations(locale);
const base = "/ja" as const;
---

<IndustriesTemplate locale={locale} t={t} base={base} />
