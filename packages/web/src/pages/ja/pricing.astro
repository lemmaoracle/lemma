---
import PricingTemplate from "../../templates/PricingTemplate.astro";
import { getTranslations } from "../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<PricingTemplate locale={locale} t={t} base={base} />
