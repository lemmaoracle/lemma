---
import PricingTemplate from "../../templates/PricingTemplate.astro";
import { getTranslations } from "../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<PricingTemplate
  locale={locale}
  t={t}
  base={base}
  title="料金 — Lemma Oracle"
  description="Lemma Oracle のエンタープライズおよび開発者向け料金。3つのセグメント、1つの信頼レイヤー。"
/>
