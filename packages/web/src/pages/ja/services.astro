---
import ServicesTemplate from "../../templates/ServicesTemplate.astro";
import { getTranslations } from "../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<ServicesTemplate locale={locale} t={t} base={base} />
