---
import AuthenticityTemplate from "../../templates/AuthenticityTemplate.astro";
import { getTranslations } from "../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<AuthenticityTemplate locale={locale} t={t} base={base} />
