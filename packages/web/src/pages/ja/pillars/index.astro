---
// /ja/pillars — the canonical "Lemma API 全体像" hub (code納品 20260707). The old
// pillars/4-axes hub was retired; the short-lived /ja/lemma-api route was removed.
import LemmaApiTemplate from "../../../templates/LemmaApiTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<LemmaApiTemplate
  locale={locale}
  t={t}
  base={base}
  pagePath="/pillars/"
  ogImage="https://lemma.frame00.com/og/pillars/ja.png"
/>
