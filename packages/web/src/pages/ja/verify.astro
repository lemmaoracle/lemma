---
import VerifyAnchorTemplate from "../../templates/VerifyAnchorTemplate.astro";
import { getTranslations } from "../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<VerifyAnchorTemplate locale={locale} t={t} base={base} />
