---
import Trust402SellTemplate from "../../../templates/Trust402SellTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<Trust402SellTemplate locale={locale} t={t} base={base} />
