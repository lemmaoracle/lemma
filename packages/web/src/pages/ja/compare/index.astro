---
/* 「他の手段との違い」比較ページ。日本語先行（EN は残タスク）。
   同ディレクトリのモデル比較 LP 3件（gpt5-vs-opus 等）とは別系統の
   手段比較ハブで、URL は /ja/compare/。 */
import CompareJaTemplate from "../../../templates/CompareJaTemplate.astro";
import { getTranslations } from "../../../i18n/translations";

const locale = "ja";
const t = getTranslations(locale);
const base = "/ja";
---

<CompareJaTemplate locale={locale} t={t} base={base} />
