// TODO: Phase 2 — implement lemma_register_document tool
// This tool wraps `documents.register(client, payload)` from @lemmaoracle/sdk.
// It requires a RegisterDocumentRequest payload with schema, docHash, cid, issuerId,
// subjectId, commitments, revocation, and optional attributes, signature, hooks.
