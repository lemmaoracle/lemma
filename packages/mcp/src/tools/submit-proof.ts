// TODO: Phase 2 — implement lemma_submit_proof tool
// This tool wraps `proofs.submit(client, payload)` from @lemmaoracle/sdk.
// It requires a SubmitProofRequest payload with docHash, circuitId, proof, inputs,
// and optional chainId, disclosure, onchain flags.
