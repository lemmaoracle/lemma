---
/**
 * Deprecated: the old sample-chooser industry icons. The Oracle Pipeline
 * UI doesn't surface industry icons (industry is shown inline on the
 * sample chips as plain text). File kept as an empty stub so deletion
 * doesn't churn anyone's local branch; remove in a follow-up cleanup.
 */
export interface Props {
  industry?: string;
}
---

<span aria-hidden="true"></span>
