node_modules/
dist/
coverage/
.env
.env.local
.DS_Store
artifacts/
cache/
typechain-types/
.pnpm-store/
*.tgz
*.tsbuildinfo

# WASM build artifacts
**/pkg/
**/target/

# Claude Code / subagent caches
.claude/

# Sandbox tmp directories (Cowork etc — show up at repo root and inside packages/)
_tmp_*

# Local forex/composite Trust402 archives (envelope + listing receipts)
**/.archives/

# Note: broadcast/ directories are NOT ignored - they contain important deployment history